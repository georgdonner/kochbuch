import { InlineHrefDirective } from './inline-href.directive';

describe('InlineHrefDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineHrefDirective();
    expect(directive).toBeTruthy();
  });
});
