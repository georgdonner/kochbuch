import { DifficultyStringPipe } from './difficulty-string.pipe';

describe('DifficultyStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyStringPipe();
    expect(pipe).toBeTruthy();
  });
});
