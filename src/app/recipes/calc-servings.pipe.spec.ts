import { CalcServingsPipe } from './calc-servings.pipe';

describe('CalcServingsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcServingsPipe();
    expect(pipe).toBeTruthy();
  });
});
